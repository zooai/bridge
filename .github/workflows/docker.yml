name: Docker

on:
  push:
    branches: [main, dev]
    tags: ['v*']

jobs:
  docker:
    uses: hanzoai/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/zoolabs/bridge
    secrets: inherit
